#nullable enable

namespace SharpCoreDB.Provider.Sync.Metadata;

/// <summary>
/// Reads existing table schemas for sync setup auto-discovery.
/// Extracts metadata: columns, primary keys, foreign keys, constraints.
/// </summary>
public sealed class SharpCoreDBSchemaReader
{
    // TODO: Implement in Phase 3.6
    // Purpose: Read existing table schemas for sync setup auto-discovery
    // Methods:
    //   - GetTablesAsync()
    //   - GetColumnsAsync(tableName)
    //   - GetPrimaryKeysAsync(tableName)
    //   - GetRelationsAsync(tableName)
}
