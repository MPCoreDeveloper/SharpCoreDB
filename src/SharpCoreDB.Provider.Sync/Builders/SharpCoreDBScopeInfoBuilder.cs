#nullable enable

namespace SharpCoreDB.Provider.Sync.Builders;

/// <summary>
/// Manages CRUD operations on Dotmim.Sync scope metadata.
/// Tracks sync state, timestamps, and scope configuration per endpoint.
/// </summary>
public sealed class SharpCoreDBScopeInfoBuilder
{
    // TODO: Implement in Phase 2
    // Purpose: CRUD operations on scope_info and scope_info_client tables
    // Methods:
    //   - EnsureScopeInfoTableAsync()
    //   - GetScopeInfoAsync(scopeName)
    //   - SaveScopeInfoAsync(scopeInfo)
    //   - GetScopeInfoClientAsync(scopeId, scopeName)
    //   - SaveScopeInfoClientAsync(scopeInfoClient)
}
