<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="using:SharpCoreDB.Viewer.ViewModels"
        xmlns:loc="using:SharpCoreDB.Viewer.Converters"
        mc:Ignorable="d" d:DesignWidth="500" d:DesignHeight="350"
        x:Class="SharpCoreDB.Viewer.Views.ConnectionDialog"
        x:DataType="vm:ConnectionDialogViewModel"
        Title="{loc:Localize ConnectionDialogTitle}"
        Width="500" Height="350"
        WindowStartupLocation="CenterOwner"
        CanResize="False"
        Icon="/Assets/SharpCoreDB.ico">

    <Design.DataContext>
        <vm:ConnectionDialogViewModel/>
    </Design.DataContext>

    <Window.Styles>
        <Style Selector="TextBlock.header">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style Selector="TextBlock.error">
            <Setter Property="Foreground" Value="Red"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
        </Style>
    </Window.Styles>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Classes="header" Text="{loc:Localize ConnectionDialogTitle}"/>
            <TextBlock Text="{loc:Localize ConnectionDialogSubtitle}"
                       Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                       FontSize="12"/>
        </StackPanel>

        <!-- Form -->
        <StackPanel Grid.Row="1" Spacing="15">
            
            <!-- Database Path -->
            <StackPanel>
                <TextBlock Text="{loc:Localize DatabasePath}" Margin="0,0,0,5"/>
                <Grid ColumnDefinitions="*,Auto,Auto">
                    <TextBox Grid.Column="0"
                             Text="{Binding DatabasePath}"
                             Watermark="{loc:Localize DatabasePathPlaceholder}"
                             IsEnabled="{Binding !IsConnecting}"/>
                    <Button Grid.Column="1"
                            Content="{loc:Localize Browse}"
                            Command="{Binding BrowseForDatabaseCommand}"
                            IsEnabled="{Binding !IsConnecting}"
                            Margin="5,0,0,0"
                            Width="80"
                            ToolTip.Tip="{loc:Localize BrowseTooltip}"/>
                    <Button Grid.Column="2"
                            Content="{loc:Localize New}"
                            Command="{Binding CreateNewDatabaseCommand}"
                            IsEnabled="{Binding !IsConnecting}"
                            Margin="5,0,0,0"
                            Width="80"
                            ToolTip.Tip="{loc:Localize NewTooltip}"/>
                </Grid>
            </StackPanel>

            <!-- Password -->
            <StackPanel>
                <TextBlock Text="{loc:Localize MasterPassword}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Password}"
                         Watermark="{loc:Localize PasswordPlaceholder}"
                         PasswordChar="?"
                         IsEnabled="{Binding !IsConnecting}"/>
            </StackPanel>

            <!-- Error Message -->
            <TextBlock Classes="error"
                       Text="{Binding ErrorMessage}"
                       IsVisible="{Binding ErrorMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                       TextWrapping="Wrap"/>

            <!-- Connection Status -->
            <StackPanel Orientation="Horizontal"
                        Spacing="10"
                        IsVisible="{Binding IsConnecting}">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4"/>
                <TextBlock Text="{loc:Localize Connecting}" VerticalAlignment="Center"/>
            </StackPanel>

        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="10"
                    Margin="0,20,0,0">
            <Button Content="{loc:Localize Cancel}"
                    Width="100"
                    Command="{Binding CancelCommand}"
                    IsEnabled="{Binding !IsConnecting}"/>
            <Button Content="{loc:Localize Connect}"
                    Width="100"
                    Command="{Binding ConnectCommand}"
                    IsEnabled="{Binding !IsConnecting}"
                    Classes="accent"/>
        </StackPanel>

    </Grid>

</Window>
